{% extends 'base.html.twig' %}

{% block title %}AdvertType{% endblock %}

{% block body %}
    <h1>AdvertType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advert_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ advert_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('advert_type_index') }}">back to list</a>

    <a href="{{ path('advert_type_edit', {'id': advert_type.id}) }}">edit</a>

    {{ include('advert_type/_delete_form.html.twig') }}
{% endblock %}
