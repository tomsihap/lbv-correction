{% extends 'base.html.twig' %}

{% block title %}AdvertType index{% endblock %}

{% block body %}
    <h1>AdvertType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for advert_type in advert_types %}
            <tr>
                <td>{{ advert_type.id }}</td>
                <td>{{ advert_type.name }}</td>
                <td>
                    <a href="{{ path('advert_type_show', {'id': advert_type.id}) }}">show</a>
                    <a href="{{ path('advert_type_edit', {'id': advert_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('advert_type_new') }}">Create new</a>
{% endblock %}
